<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Admin Dashboard</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      text-align: center;
      margin: 50px;
    }
    h2 {
      color: #333;
    }
    p {
      color: #666;
    }
    a {
      color: #007bff;
      text-decoration: none;
      margin: 5px;
    }
    .admin-links, .user-links {
      display: block; /* Initially show these links */
    }
    .user-links, .admin-info {
      display: block; /* Initially hide these links */
    }
    .toggle-links-button {
      padding: 10px;
      margin: 10px;
      cursor: pointer;
    }
  </style>
</head>
<body>


 <div class="admin-links">
  <% if admin_signed_in? %>
    <% current_member = Member.find_by(email: current_admin.email) %>

    <% if current_member&.position != 'Member' %>
      <div class="links">
        <h2>Admin Dashboard</h2>
        <p>Welcome to the main dashboard page.</p>
        <p>Here, you can provide options or links specific to logged-in admins.</p>
        <%= link_to 'View Members', members_path %><br>
        <%= link_to 'View Officer Members', officer_index_members_path %><br>
        <%= link_to 'View Events', events_path %><br>
        <%= link_to 'View Attendance', attendances_path %><br>
        <%= link_to 'View Officer Entries', officer_entries_path %><br>
        <%= link_to 'View Finances', finances_path %><br>
      </div>
    <% else %>
      <div class="admin-info">
        <h2>Member Dashboard</h2>
        <p>Welcome to the main dashboard page.</p>
        <div class="links">
          <%= link_to 'View Members', members_path %><br>
          <%= link_to 'View Officer Members', officer_index_members_path %><br>
        </div>
      </div>  
    <% end %>
  <% end %>
</div>


  <script>

    document.addEventListener("DOMContentLoaded", function() {
      var adminLinks = document.querySelector('.links');
      var userLinks = document.querySelector('.user-links');
      var adminInfo = document.querySelector('.admin-info');
      var toggleButtons = document.querySelectorAll('.toggle-links-button');

      toggleButtons.forEach(function(button) {
        button.addEventListener('click', function() {
          console.log(adminInfo.style.display); 
          adminLinks.style.display = adminLinks.style.display === 'none' ? 'block' : 'none';
          if(adminInfo.style.display == 'none' || adminInfo.style.display == ''){
            adminInfo.style.display = 'block'; 
          }
        else{
          adminInfo.style.display = "none"; 

        }
        });
      });
    });
  </script>
</body>
</html>
