<p style="color: green"><%= notice %></p>
<h2>Member Information</h2>
<%= render @member %>

<br>
<br>
<br>
<h2>Member Attendance</h2>

<table>
  <thead>
    <tr>
      <th>Event Name</th>
      <!-- Add other relevant headers -->
    </tr>
  </thead>
  <tbody>
    <% @member_attendances.each do |attendance| %>
      <tr>
        <td><%= attendance.event_name %></td>
        <!-- Display other attendance attributes as needed -->
      </tr>
    <% end %>
  </tbody>
</table>

<div class="links">
  <%= link_to "Edit this member", edit_member_path(@member) %> |
  <%= link_to "Back to members", members_path %>
</div>
<div class="box"> </div>
