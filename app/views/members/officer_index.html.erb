<!-- app/views/members/index.html.erb -->

<style>
  .link-like-button {
    background: none !important;
    border: none;
    padding: 0 !important;
    font-family: arial, sans-serif;
    color: #069;
    text-decoration: underline;
    cursor: pointer;
  }

  .link-like-button:hover {
    color: #00C;
    text-decoration: underline;
  }
</style>

<h1>Officers</h1>
<table>
  <thead>
    <tr>
      <th>ID</td>
      <th>First Name</th>
      <th>Last Name</th>
      <th>Position</th>
      <th>Officer Points</th>
      <th colspan="3"></th>
    </tr>
  </thead>
  <tbody>
    <% @members.each do |member| %>
      <tr>
        <td><%= member.id %></td>
        <td><%= member.nameFirst %></td>
        <td><%= member.nameLast %></td>
        <td><%= member.position %></td>
        <td><%= member.officer_points %></td>
        <td><%= link_to 'Add Officer Points', edit_officer_points_member_path(member) %></td>
      </tr>
    <% end %>
  </tbody>
</table>
<%= link_to "Back to members", members_path %>
<%= link_to 'Bulk Add Officer Points', bulk_edit_officer_points_members_path %>


<script>
document.addEventListener("DOMContentLoaded", function() {
  var deleteForms = document.querySelectorAll('.delete-member-form');

  deleteForms.forEach(function(form) {
    form.addEventListener('submit', function(e) {
      if (!window.confirm('Are you sure you want to delete this member?')) {
        e.preventDefault();
      }
    });
  });
});
</script>


